import { StyleSheet } from "react-native";

export const styleListProduct = StyleSheet.create({
  content: {
    flex: 1,
    paddingHorizontal: 10,
    backgroundColor: "#fff",
    paddingBottom: 20,
    marginBottom: 20,
  },
});
